Perfetto, le tue risposte definiscono molto bene la direzione. Riassumo brevemente **l'architettura che stai implicitamente scegliendo**, così il piano che segue è coerente:

### Architettura risultante

Il sistema diventa una **AI Agent UI basata su workflow dinamici**:

* **taskType sempre `generic`**
* `taskName` descrive il task
* **workflow dinamico**
* **step generati uno alla volta dal modello**
* **step influenzati dagli step precedenti**
* **stato completo salvato nel client**
* **input UI predefiniti**
* **step con massimo pochi input**
* **step history persistita**
* **utente può tornare indietro**
* **sessione persistente**
* **stimatore di step rimanenti**
* **logging completo**

Questo è molto vicino a un **AI-driven workflow engine**.

---

# Plan: Multistep

## Overview

Upgrade the existing Shared State UI prototype into a **generic AI-driven multi-step workflow engine**.
The system no longer relies on predefined task types and instead supports **fully generic tasks** generated by Gemini. Each interaction is modeled as a **dynamic step** within a workflow, where the model generates the next step based on the previous user input and accumulated state.

The system maintains **complete step history**, allowing backward navigation, session persistence, and state-aware reasoning. Steps can contain **multiple UI inputs** selected from a predefined set of accessible components.

The architecture emphasizes:

* Generic AI task orchestration
* Dynamic multi-step workflows
* Persistent shared state
* Accessible UI rendering
* Structured model outputs
* Step history replay
* Workflow estimation
* Logging and debugging

The final system behaves as an **AI Agent UI**, enabling users to complete complex workflows through incremental interaction.

---

# Validation Commands

* `npm run lint`
* `npm run test`
* `npx playwright test`

---

# Task 1: Redesign Structured Output Schema

* [ ] Open `app/actions/processUserInput.js`
* [ ] Replace the previous response schema with a **generic workflow schema**

New required fields:

```
taskId: string
taskType: "generic"
taskName: string
stepId: string
stepNumber: number
estimatedRemainingSteps: number
stateSummary: string
inputs: array
```

Each input object must contain:

```
id: string
type: enum(
  text_input,
  number_input,
  select_option,
  multi_select,
  boolean_confirm,
  date_input,
  file_upload,
  rating,
  slider
)

label: string
options?: string[]
placeholder?: string
required?: boolean
```

* [ ] Remove `pendingAction`
* [ ] Update Gemini prompt instructions to explain the workflow format
* [ ] Ensure `responseMimeType: "application/json"`
* [ ] Mark completed

---

# Task 2: Introduce Workflow State Model

* [ ] Open `app/store/useSharedState.js`
* [ ] Add new state variables:

```
workflow: {
  taskId: null,
  taskName: "",
  steps: []
}

currentStepIndex: 0
estimatedRemainingSteps: null
```

* [ ] Each step stored as:

```
{
 stepId,
 stepNumber,
 questionSummary,
 inputs,
 response
}
```

* [ ] Add actions:

```
addStep(step)
updateStepResponse(stepId, response)
goToPreviousStep()
resetWorkflow()
setEstimatedSteps(n)
```

* [ ] Ensure state persists using Zustand `persist`
* [ ] Mark completed

---

# Task 3: Build Step History Management

* [ ] Create file `app/utils/workflowHelpers.js`

Add helper functions:

```
buildConversationMemory(steps)
extractStepResults(steps)
getCurrentStep(steps, index)
```

* [ ] Ensure the full step history is passed to the model
* [ ] Format history like:

```
Step 1 Question
Step 1 Response
Step 2 Question
Step 2 Response
```

* [ ] Mark completed

---

# Task 4: Update Server Action for Step-based Workflow

* [ ] Open `processUserInput.js`
* [ ] Modify function signature:

```
processWithGemini(userInput, systemContext, workflowState)
```

* [ ] Build prompt including:

```
systemContext
workflow history
last user response
```

* [ ] Request the **next step only**

* [ ] Parse and validate response

* [ ] Return new step object

* [ ] Keep retry loop (max 2 attempts)

* [ ] Log failures to:

```
logs/gemini-errors.log
```

* [ ] Mark completed

---

# Task 5: Create Generic Multi-Input Renderer

* [ ] Replace `DynamicTaskRenderer.jsx`

* [ ] Rename to `DynamicStepRenderer.jsx`

* [ ] Accept prop:

```
inputs: array
```

* [ ] Iterate through inputs:

```
inputs.map(input => renderInput(input))
```

* [ ] Implement renderer switch:

```
text_input
number_input
select_option
multi_select
boolean_confirm
date_input
file_upload
rating
slider
```

* [ ] Ensure each input:

* has `Label`

* correct `htmlFor`

* accessible focus order

* semantic grouping when needed

* [ ] Mark completed

---

# Task 6: Support Multiple Inputs per Step

* [ ] Create local component state:

```
stepResponses
```

* [ ] Update state on each input change

* [ ] Ensure response object format:

```
{
 inputId: value
}
```

Example:

```
{
 location: "Rome",
 travelType: "Train"
}
```

* [ ] Validate required inputs before submit
* [ ] Mark completed

---

# Task 7: Build Step Container UI

* [ ] Update `DynamicTaskContainer.jsx`

* [ ] Rename to `WorkflowStepContainer.jsx`

* [ ] Render:

```
Task name
Step number
Estimated remaining steps
State summary
```

* [ ] Display progress indicator:

```
Step X
~Y steps remaining
```

* [ ] Render `DynamicStepRenderer`

* [ ] Add buttons:

```
Submit Step
Previous Step
```

* [ ] Mark completed

---

# Task 8: Implement Step Navigation

* [ ] Add previous step button

Logic:

```
if(currentStepIndex > 0)
  goToPreviousStep()
```

* [ ] Load previous responses into inputs

* [ ] Disable editing past steps unless returning to them

* [ ] Mark completed

---

# Task 9: Step Submission Logic

* [ ] On submit:

1. Save response in store
2. Update step history
3. Send step result to Gemini
4. Receive next step
5. Append step to workflow
6. Move `currentStepIndex`

* [ ] Update estimated remaining steps

* [ ] Mark completed

---

# Task 10: Update Main Page Logic

* [ ] Modify `app/page.js`

Flow becomes:

```
Context Setup
↓
User prompt
↓
Step 1 generated
↓
Workflow steps
```

* [ ] When first prompt submitted:

call Gemini with empty workflow

* [ ] Save first step

* [ ] Render `WorkflowStepContainer`

* [ ] Mark completed

---

# Task 11: Session Persistence & Reset

* [ ] Zustand persist already enabled

* [ ] Add button in UI:

```
Reset Workflow
```

* [ ] On click:

```
resetWorkflow()
setSystemContext("")
```

* [ ] Return to ContextSetup screen

* [ ] Mark completed

---

# Task 12: Accessibility Improvements

* [ ] Ensure:

```
aria-live updates on step change
focus management for step title
fieldset for grouped inputs
```

* [ ] Move focus automatically to step heading

* [ ] Ensure keyboard navigation across inputs

* [ ] Mark completed

---

# Task 13: Logging System

* [ ] Create file:

```
logs/workflow-history.log
```

* [ ] Log:

```
timestamp
taskId
stepNumber
userResponse
modelResponse
```

* [ ] Use `fs.appendFileSync`

* [ ] Mark completed

---

# Task 14: End-to-End Workflow Testing

* [ ] Update `e2e/user-flow.spec.js`

Add tests:

### Test 1

Context initialization works

### Test 2

First step generation works

### Test 3

Step submission generates next step

### Test 4

Previous step navigation works

### Test 5

Workflow persistence after reload

### Test 6

Accessibility validation with axe

* [ ] Run:

```
npx playwright test
```

* [ ] Mark completed

---

# Task 15: Workflow Debug Console

* [ ] Add optional debug panel

Show:

```
workflow state
step history
current responses
estimated steps
```

* [ ] Render only in development mode

```
process.env.NODE_ENV === "development"
```

* [ ] Mark completed